{
  "name": "agent-delegation-mcp",
  "description": "Delegate coding and research tasks to the OpenCode and Antigravity CLIs from Claude Code",
  "owner": {
    "name": "Arthur Carroll"
  },
  "plugins": [
    {
      "name": "agent-delegation",
      "description": "Delegates long-running coding and research tasks to the OpenCode and Antigravity (agy) CLIs as unattended MCP subagents. Dispatch returns a run id immediately and records the run on disk, so a delegate stays reachable - checkable, tailable, cancellable - across dropped connections, session ends and server restarts. Fails fast on provider quota walls instead of blocking for the full hour, kills hung delegates on an idle timer, and reports whatever the delegate wrote to .agent-runs/ on every exit path.",
      "version": "1.1.0",
      "source": "./",
      "author": {
        "name": "Arthur Carroll"
      }
    }
  ]
}
